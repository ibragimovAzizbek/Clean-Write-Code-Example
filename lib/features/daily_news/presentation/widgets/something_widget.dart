import 'package:flutter/material.dart';

class SomethingWidget extends StatelessWidget {
  const SomethingWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
